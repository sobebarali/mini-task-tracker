# ==============================================
# Docker Compose Environment Configuration
# ==============================================
# Copy this file to .env and update the values

# Node Environment (development, production, test)
NODE_ENV=production

# Server Configuration
PORT=3000

# ==============================================
# JWT Configuration
# ==============================================

# IMPORTANT: Change this in production! Must be at least 32 characters
JWT_SECRET=your-secret-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# ==============================================
# CORS Configuration
# ==============================================

# Comma-separated list of allowed origins. Use "*" for development only.
# Production example: https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=*

# ==============================================
# Database Configuration (for Docker Compose)
# ==============================================
# Note: These are managed by docker-compose.yml
# MongoDB and Redis hostnames use service names (mongodb, redis)
# No need to configure DATABASE_URL or REDIS_HOST here
